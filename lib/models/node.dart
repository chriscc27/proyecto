class Node {
  String id;
  String name;
  double x; // Coordenada X (pantalla)
  double y; // Coordenada Y (pantalla)

  Node({required this.id, required this.name, required this.x, required this.y});
}